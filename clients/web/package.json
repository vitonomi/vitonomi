{
  "name": "@vitonomi/web",
  "version": "0.0.0",
  "private": true,
  "description": "vitonomi PWA — Next.js App Router, mobile-ready (AGPL-3.0). Full scaffold lands in Phase 6.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vitonomi/core": "*"
  }
}
