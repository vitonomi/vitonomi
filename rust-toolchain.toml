[toolchain]
# edition 2024 was stabilized in Rust 1.85; bumped to 1.86 because
# many modern crates (icu_*, idna_adapter, reqwest's URL stack)
# now require it.
channel = "1.86.0"
components = ["rustfmt", "clippy", "rust-src"]
profile = "default"
