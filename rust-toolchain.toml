[toolchain]
# edition 2024 was stabilized in Rust 1.85; bumped to 1.88 because
# the rustls + rcgen + asn1-rs + time stack requires it.
channel = "1.88.0"
components = ["rustfmt", "clippy", "rust-src"]
profile = "default"
