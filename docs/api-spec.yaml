openapi: 3.1.0
info:
  title: vitonomi hub API
  version: 0.0.1
  description: |
    OpenAPI 3.1 contract between vitonomi clients (PWA, CLI, mobile,
    browser ext) and the vitonomi-hub. Stub placeholder; full
    surface lands in Phase 4 of the implementation plan. See
    docs/README.md for the spec-suite reading order.

    All endpoints will live under /v1/ once defined.

paths: {}

components:
  schemas: {}
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: opaque-session-token
