{
  "name": "@vitonomi/cli",
  "version": "0.0.0",
  "private": true,
  "description": "vitonomi CLI — vault/hub/relay subcommands plus seed-phrase recovery (AGPL-3.0)",
  "license": "AGPL-3.0-only",
  "type": "module",
  "bin": {
    "vitonomi-cli": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vitonomi/core": "*"
  }
}
