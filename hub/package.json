{
  "name": "@vitonomi/hub",
  "version": "0.0.0",
  "private": true,
  "description": "vitonomi hub control-plane server — registry, head pointers, key blobs, alias directory (AGPL-3.0). Implementation lands in PROJECT.md Phase 4.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "bin": {
    "vitonomi-hub": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vitonomi/core": "*"
  }
}
